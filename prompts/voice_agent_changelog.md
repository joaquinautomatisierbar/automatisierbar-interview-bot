# Voice Agent Script / Config Changelog

Newest first. Each entry: `vN · YYYY-MM-DD · what changed · why`. The live Vapi assistant
(`0a7576f1-35ac-4293-977e-09b6fc3b5923`) always reflects the latest version.

- **v14 · 2026-06-11 · ASR Treuhand-Vokabular + Kontext-Plausibilitätscheck** — live-test feedback (Joaquin): nova-3 (de-CH) verhörte sich bei Fachbegriffen ("Offerten"→"Aufwertungen"; Unsinn "Meinungen schreiben" bei einem Treuhand-Büro). (1) `transcriber.keyterm` um Treuhand/Business-Vokabular erweitert (Offerten, Offerte, Steuererklärung, MWST, Jahresabschluss, Debitoren, Kreditoren, Mahnwesen, Rechnungen, Mandate, Lohnabrechnung, Revision, Bilanz). (2) §"Bei Unklarheit" — Lena nutzt jetzt den Branchen-Kontext: ergibt ein gehörtes Wort im Kontext offensichtlich keinen Sinn (wahrscheinlich akustisch falsch), übernimmt sie es nicht, sondern fragt kurz nach statt zu raten. Separat (Cockpit-Klassifizierer, nicht Lena): hangup nur noch bei Sofort-Ablehnung ohne jedes Gespräch — ein Gespräch, das erst später kippt, ist COLD.
- **v13 · 2026-06-11 · Identity-question handling + un-truncate responses** — live-test feedback (Joaquin): Lena ignored "Mit wem rede ich?" (no identity handler → "sie checkt garnicht was ich sage") and cut off mid-sentence. Two fixes: (1) §"Ehrlichkeit / KI-Frage" extended → explicit "Wer bist du / mit wem rede ich / von welcher Firma" handling (answer SOFORT + klar: "Ich bin Lena von automatisierbar — wir helfen Schweizer KMU…", never dodge) + a KRITISCH rule: always respond to the person's actual question FIRST, before continuing the flow, never ignore a question. (2) `maxTokens` 90→150 so a 1-2 sentence reply is never hard-truncated mid-sentence (the prompt's length guard still keeps her brief). No-answer detection + cold classification confirmed working in the same test.
- **v12 · 2026-06-07 · Personalized opener (mentions the company)** — Joaquin: opener jumped straight to "darf ich eine Frage stellen" without signaling she knows who she's calling. firstMessage now: "Grüezi, hier ist Lena von automatisierbar. Ich rufe kurz bei **{{firma}}** an — darf ich Ihnen ganz kurz eine Frage stellen?" — self-intro + company name (shows it's a prepared call, not a generic robocall) while staying short (~6s, no return to the v8 monologue). §1 updated accordingly (was "NICHT die Firma vorlesen" — that v4-era rule is reversed now) + added wrong-company handling ("bin ich richtig bei…/Sie sind falsch" → apologize + ask for right place/person). Note: {{firma}} is set by the dialer (Workflow D) from the lead; in manual tests it shows the test label (e.g. "Joaquin (Test)").
- **v11 · 2026-06-07 · Faster responses + kill idle-spam + short close that actually hangs up** — Joaquin v10 call hit `exceeded-max-duration` (8 min) again. Three issues, two of them my own overcorrection from v10: (1) **idle-message spam** — "Sind Sie noch dran?"/"Können Sie mich noch hören?" fired ~8× (amplified by the long response gaps + ASR missing Joaquin's replies). (2) **latency too high** — v10's patient endpointing (wait 0.8 / noPunct 2.0 / number 1.5) created 3–7s response gaps ("geht zu lange zum antworten"). (3) **booking close dragged forever and never hung up** — day→time (9-vs-11 confusion)→channel→email→spell-back→re-confirm, then ran out the clock. Fixes: **endpointing rebalanced** (wait 0.8→0.5, noPunct 2.0→1.2, punct 0.6→0.4, number 1.5→1.0 — faster than v10, not trigger-happy like pre-v10); **idle way down** (idleTimeoutSeconds 12→20, idleMessageMaxSpokenCount 2→1); **short close** (§5 rewritten) — *we already have their number* (we dialed them), so default to "reicht eine SMS auf diese Nummer, oder E-Mail?"; email = ask once + one read-back then fall back to SMS, no endless spelling; once day+time fixed → **immediately say goodbye and hang up**; **`endCallPhrases`** set (["bis dann","Schönen Tag noch","Auf Wiederhören","Adieu"]) so Lena actually ends the call (per Joaquin: "hängt nie ab… irgendwann ist mal gut"); **Saturday softened** (offer a weekday once, but if they insist take it — no "da arbeiten Sie ja eh nicht"). maxTokens stays 90.
- **v10 · 2026-06-07 · Stop interrupting + stop truncating + drive to the Termin** — Joaquin v9 test exposed that v9's `maxTokens 60` was too low: bot turns truncated **mid-word** ("…die rechte", "…dauert das p", "…unverbindlic", "Im besten Fal") — that was the "stoppt mittendrin." Also bot barged in before the user finished (overlapping timestamps; Joaquin said it live: "lassen Sie mich aussprechen"), over-used "Verstehe/Okay/Aha", and never reached a concrete booking (asked "Hätten Sie Interesse?" 2×, never proposed a time). Fixes: **maxTokens 60 → 90** (full short turns, no mid-word cut; brevity prompt rule from v9 keeps them short anyway); **endpointing made patient** — startSpeakingPlan waitSeconds 0.4→0.8, transcriptionEndpointing onPunctuation 0.3→0.6 / onNoPunctuation 1.1→2.0 / onNumber 0.5→1.5 (she now waits, esp. after numbers where people pause) + prompt "Lass die Person IMMER ausreden, fall ihr NIE ins Wort"; **backchanneling softened** in prompt ("nicht jede Antwort mit Verstehe/Okay — wirkt wie eine Masche"; backchannelingEnabled already false); **drive to Termin** — ask interest only ONCE, once pain + rough time exist stop digging and go straight to the concrete appointment proposal. *Note:* slightly higher response latency is the deliberate trade for not interrupting. Transcriber stays Deepgram de-CH (operator decision: Swiss High German coverage is enough; broad Mundart accepted as edge case).
- **v9 · 2026-06-06 · Kill the opening monologue (shorter turns)** — Tej v8 call, measured from message timing: bot turns were **9.2s, 15.8s, 14.8s** — Lena monologued ~16s before the prospect could react ("redet zu lange am Anfang, fast 20s"). Root cause: `maxTokens 120` ≈ 80–90 German words ≈ ~15s of speech. Fixes: **maxTokens 120 → 60** (physical cap ~6–8s/turn); **firstMessage shortened** to "Grüezi, hier ist Lena von automatisierbar. Darf ich Ihnen ganz kurz eine Frage stellen?" (context moved to the now-short turn 2); **§2 bridge cut** to one short sentence + question ("Ganz kurz: Wir helfen KMU, lästige Büroarbeit zu automatisieren. Was frisst bei Ihnen am meisten Zeit?"); Sprechweise hard rule "max 1–2 kurze Sätze / ~6s pro Redebeitrag, nie ein Monolog, spiegeln knapp statt ganzen Satz nacherzählen." Steering from v8 confirmed working in-call ("plaudern Sie mit?" → "Haha, gern ein andermal, bleiben wir beim Thema"). *Still open (needs creds):* **Schweizerdeutsch dialect** — v8/v9 Tej call confirms `de-CH` does NOT handle broad Mundart (transcript fully garbled). Hard blocker for dialect speakers; next lever = Azure/Speechmatics `de-CH` (provider key required), or accept that dialect speakers must shift to Swiss High German.
- **v8 · 2026-06-06 · Conversation control (no tangents) + warmer tone** — Tej troll-test exposed two things: (1) **zero steering** — Lena followed every off-topic tangent ("Soll ich dir erzählen was Isabelle für mich macht?" → "Ja, gerne!"), chatted about private/physical/irrelevant stuff, and the call ran to **max-duration (8 min, $0.91)** without ever redirecting or closing. (2) **tone sometimes reads as genervt** (Nico flagged this earlier too). Added a "# Gesprächsführung — Fokus halten, Tangenten freundlich zurücklenken" section: lead toward the one goal (pain → qualify → 30-min Termin), acknowledge a tangent in ONE warm line then redirect (no follow-ups on the tangent, no plaudern), decline "soll ich dir erzählen…" offers, stop re-litigating a point after twice, and the illegal/"graue" request → calm hard-no + steer back/close (Tej's "schwarzes Geschäft" ask was already declined well — now explicit). Tone: added "IMMER warm/geduldig, NIE genervt/ungeduldig, auch beim Wiederholen/Zurücklenken." Voice: ElevenLabs **stability 0.4 → 0.55** (calmer, less clipped/erratic prosody that read as impatient) — reversible if disliked. *Still open (needs creds, not promptable):* full **Schweizerdeutsch dialect** — `de-CH` (v7.2) handles Swiss *Standard* German but Tej's broad Mundart still failed (he switched to Hochdeutsch). Next lever = Azure or Speechmatics `de-CH` transcriber, both require a provider API key wired into Vapi.
- **v7.2 · 2026-06-06 · Swiss German transcription (Deepgram `de` → `de-CH`)** — Tej test call: he spoke Schweizerdeutsch and Deepgram (language `de` = Hochdeutsch) garbled it badly ("Murik" → "Re-mourik"; whole turns dropped → Lena stuck on "Sind Sie noch dran?"). Per business-context Prior (f), real prospects speak Schweizerdeutsch, so this is the highest-impact fix for live calls. Switched transcriber language to Deepgram's dedicated **`de-CH` (German Switzerland)** locale — same provider (no new credential; none for Google/Azure/Speechmatics exist in `.env` anyway), same nova-3 model + keyterms, same low latency. Accepted by Vapi (nova-3 + de-CH = HTTP 200). *Honest caveat:* de-CH targets Swiss **Standard** German (Schweizer Hochdeutsch); heavy Mundart dialect stays hard for ALL ASR — but de-CH is a real step up from generic `de`, and phone prospects often shift toward Swiss High German. If still weak after re-test, next options = Azure/Speechmatics de-CH (both need creds wired). *Validate:* re-call a Schweizerdeutsch speaker (Tej) and compare.
- **v7.1 · 2026-06-06 · Bookable: appointment fields in structured-data extraction** — Lena booked verbally but nothing structured came out for a confirmation/scheduling backend to act on. Extended the assistant's `analysisPlan.structuredDataPlan.schema` (was: connected/interest_level/pain_score/interview_accepted/etc.) with 5 booking fields: `appointment_booked` (bool — only when day+time agreed AND confirmed), `appointment_day` (text as said), `appointment_time` (24h), `contact_channel` (sms/email/phone), `contact_value` (email/number; "inbound_number" if prospect says "use the number you called"). These land in `call.analysis.structuredData` on the existing `end-of-call-report` webhook → `https://oojoaquin.app.n8n.cloud/webhook/voice-call-result`. Handoff for the in-progress confirmation backend: read those fields; resolve `appointment_day`+`appointment_time` to a real datetime using the call's `createdAt` (extractor doesn't know "today"). Caller-ID/Swiss-number + SMS/confirmation sender being handled separately by operator (Swiss number ~2 days out). *Why:* "she needs to be able to book an appointment to talk about it again" — turns the conversation into consumable booking data.
- **v7 · 2026-06-06 · Honest meeting framing + natural scheduling + scale fix** — Nico v6-call feedback. (1) **Meeting = discovery, not demo.** Lena had over-promised that the 30-min call shows "wie alles funktioniert / was sich ändert" + a finished prototype — but that call is the *Workflow Interview* (business-context sales motion step 3 = understand the process), build happens *after* (step 4), demo is a *later* meeting (step 5). Added a "Was im Termin wirklich passiert" block enforcing the honest order: erst VERSTEHEN (im Termin) → dann BAUEN (danach) → dann unverbindlich TESTEN; explicit "nicht im Termin einen fertigen Prototyp / 'wie alles funktioniert' versprechen." (2) **Scheduling less like an interrogation.** Was 4 sequential questions (week → day → vormittags/nachmittags → exact time). Now: week → day → **one concrete proposal** ("[Tag] am Vormittag, so gegen 9 Uhr? Sonst sagen Sie, was besser passt") → read back once. Per Nico: a suggestion the prospect adjusts sounds far more natural than asking everything. (3) **Scale drift fixed** — Lena improvised "Skala 1 bis 10" in the v6 call; hardened to "IMMER 1 bis 5, nie 1 bis 10." *Open infra (not prompt — flagged to operator):* (a) **caller ID is a US Twilio number** `+1 814 914 9096` (only *named* "Automatisierbar +41 44") → Swiss carriers mislabel it ("Corsica") + low pickup; needs a real +41 number (Twilio CH regulatory bundle). (b) **No SMS/booking-confirmation backend exists** — Lena promised an SMS that nothing sends; booking data dies in the transcript. Needs build: extract booking from call → send SMS/email + optional calendar event.
- **v6 · 2026-06-06 · Objection handling: convert the soft "no" (risk-reversal loop)** — Nico v5-call feedback: he gave an unspecific "keine Interesse" (no real reason) and Lena folded on the first no — "many prospects who don't understand AI need a bit of convincing." Added §4b: a soft/reasonless "no interest" is treated as a **MAYBE, not a NO** (Hormozi "money is in the maybes" / Power & Decision Avoidance), handled with **one** Looping pass (Acknowledge → Add reframe → re-Ask) using the honest **risk-reversal** ("Sie haben nichts zu verlieren — wir bauen Ihnen was, Sie testen es unverbindlich, zahlen keinen Franken, sind zu nichts verpflichtet; worst case 30 Min investiert, best case Stunden/Woche gespart" — the "Both Options Are Risk-Free" / "Best Case–Worst Case" closes, *ACQ Closer Handbook*), then **one** re-ask for the 30-min look. Hard guardrails: **exactly one** attempt then the person decides; firm no → accept immediately (no second push, no imperatives, per operator tone rules); concrete reasons (time/authority/budget) routed to their own handling; **no invented guarantee, no fixed prices** (guarantee is TBD in business-context §1/§9 — Lena may only state the *real* free-pilot/no-obligation model, Prior c). Grounded against `references/business-context.md` (build-before-pilot + free 2-week pilot confirmed true) + `library` skill (sales-and-closing). Also bumped `messagePlan.idleTimeoutSeconds` 10→12 (v5 idle prompt fired ~1s early while Nico was mid-answer). *Why:* win undecided prospects who need light convincing without becoming pushy or dishonest.
- **v5 · 2026-06-06 · Opener-framing + robust booking tail + graceful silence** — live test calls to Patrik & Nico. Three fixes from Nico's feedback ("Anfang: unklar was sie will; Ende: Funkstille beim Terminieren; Mitte: top"): (A) **opener no longer presumes** — after the gruss, Lena gives a one-sentence "worum's geht" bridge + an **open** question ("Was frisst bei Ihnen im Büro am meisten Zeit?"); the branche-specific guess is demoted to an *example* only if the person hesitates. Baked in the "Was wollen Sie verkaufen? / Warum rufen Sie an?" objection-handling up front (it was a mid-call recovery before). (C) **step-by-step Termin & Erreichbarkeit sub-flow** — day → part-of-day → exact time (read back once; clarify "4 Uhr" → nachmittags?) → contact channel → email spelled back & confirmed; don't end until day+time+contact confirmed, else offer callback. Fixes email-capture dead-end seen in *both* Patrik & Nico calls. (B) added `messagePlan.idleMessages` (warm: "Sind Sie noch dran?", maxSpokenCount 2, idleTimeout 10s) + `silenceTimeoutSeconds 30` + prompt rule "no annoyed tone / no clipped repeat on silence" — replaces Lena improvising a clipped "Hallo, bin noch da". *Why:* Nico perceived the latency-gap + clipped re-ask as "genervt". Canonical prompt re-synced to `prompts/voice_agent_system.txt` (was stale at pre-v4). *Open:* dropped-"ja"→double-AI-turn (turn-taking/endpointing) left untouched on purpose — validate via re-test before tuning startSpeakingPlan.
- **v4 · 2026-06-02 · Humanization pass** — spoken-German "Sprechweise" block (bad/good example pairs, contractions, one-idea-per-turn) to kill written-register stiffness; backchannels + word-mirroring; shorter natural opener ("Grüezi…", no firma read-back); **proactive AI disclosure removed** (truthful only if asked); voice → `eleven_flash_v2_5`, style 0.0, speed 1.0 (less performed, lower latency); maxTokens 250→120; Deepgram keyterms (Treuhand/Mandant/Fristen/…); backchannelingEnabled. Also wired `server.url` → Workflow E + activated E (loop closed). *Why:* operator feedback "works but still clearly AI" after first live pipeline call.
- **v3 · 2026-06-01 · Naturalness + turn-taking pass** — reverted Azure→ElevenLabs native German voice (`eleven_multilingual_v2`, stability 0.4 / style 0.4 / speed 1.08); enabled Vapi **smart (semantic) endpointing** so short answers + pauses don't get cut off; added "lies NICHT Wort für Wort ab" + "nicht wiederholt entschuldigen" rules; firstMessage uses periods (cleaner TTS pacing). *Why:* test call 2 (Azure de-CH) was robotic + slow + choppy turn-taking.
- **v2 · 2026-06-01 · Siezen + tightening** — enforced strict "Sie" (no duzen), shorter replies (maxTokens 250), faster turn-wait, backgroundSound off, tried Azure Swiss voice (later reverted). *Why:* test call 1 slipped from "Sie" into "du", had ambient noise + lag.
- **v1 · 2026-06-01 · Initial build from Skript v2** — deployed assistant from `prompts/voice_agent_conversation.md`: German Skript v2 flow, branche-specific Q1, recording OFF, post-call analysis schema, Claude Haiku 4.5, Deepgram de transcriber.
